apiVersion: v1
kind: Service
metadata:
  name: {{ template "eseauthapi.fullname" . }}
  labels:
    app: {{ template "eseauthapi.name" . }}
    chart: {{ template "eseauthapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "eseauthapi.name" . }}
    release: {{ .Release.Name }}
