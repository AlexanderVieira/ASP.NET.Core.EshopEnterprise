apiVersion: v1
kind: Service
metadata:
  name: {{ template "esestoremvc.fullname" . }}
  labels:
    app: {{ template "esestoremvc.name" . }}
    chart: {{ template "esestoremvc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "esestoremvc.name" . }}
    release: {{ .Release.Name }}
